# Audit: 1df67f6

Reviewed 34 landed commits at integrated revision
`1df67f67e955238eba3829901b3bd69e7f43db40`, covering
`3bf67c52ca1ab1d03909957e6705a743b6456a3f..1df67f67e955238eba3829901b3bd69e7f43db40`.
This is a fix-forward audit; landing remains settled.

## Review and findings

Inspected the direct USDC profiles, shared authorization and Solana integration,
recipient ATA creation, faucet_ex migration, x402 client URL handling and server
binding, critical coverage, method grammar/HMAC vectors, QA aliases, dependency
and runtime pins, documentation, release notes, SDK metadata and prior audits.
No reviewer rejections were supplied. No additional debug-output defect was found.

Six findings, three fixed:

1. **Fixed — USDC analyzer hygiene.** Baseline ExDNA found one exact clone:
   recipient validation in EVM and Solana. Moved the identical validation into
   `USDC.Binding.require_recipient/1`. Reach flagged repeated list indexing in
   Solana instruction classification; build an index map once and use map lookups.
   Added a regression covering out-of-range program and account indexes, retaining
   the same error before RPC. Dialyzer found an unreachable successful-receipt
   fallback in `USDC.Solana.broadcast/5`; removed it, preserving the successful
   receipt and tagged error cases. All three analyzer checks pass after repair.
2. **Fixed — unsupported config accepted at boot.** USDC Solana accepted `localnet`
   in config despite having no native asset registry entry and rejecting every
   localnet challenge. Restrict its networks and error messages to mainnet/devnet;
   add a boot-validation regression. Generic Solana localnet support is unaffected.
3. **Fixed — redundant test assertion.** Elixir reported the facilitator function
   shape assertion as statically true. Removed those two redundant lines while
   preserving transport, error and response tests. The supplied prior b668a48
   audit claimed this fix, but ground-truth HEAD still contained it; repaired it
   here rather than assuming that earlier audit had landed.
4. **Tracked — stale mutation evidence.** The full suite again confirmed
   `fingerprint_mismatch` at `test/mutation/security_campaign_test.exs:33`.
   The fingerprint hashes complete mutation definitions and source/test inputs;
   landed changes have invalidated the recorded campaign. Updated **Task 141**
   with this revision's evidence. No ledger hash, threshold or test was weakened.
   Executing and reviewing the real campaign remains normal implementation work.
5. **Tracked — missing freshness prerequisites.** Both external host scripts still
   skip. Updated **Task 136** with recurrence evidence; exit zero is not proof of
   freshness.
6. **Tracked — release-note omissions.** Existing **Task 142** owns missing x402
   migration/coverage notes. Expanded it to cover shipped USDC direct profiles,
   `Authorization.settle/3` and recipient ATA creation. CHANGELOG remains untouched.

No new task was necessary: unresolved causes were semantically deduplicated into
Tasks 136, 141 and 142, retaining catalog routing `codex` / `gpt-6-astra`.
The specific audit repair-task instruction authorizes these task updates and their
rendered artifacts. Task 129 continues to own module-map completeness. A read-only
private-inbox query succeeded; existing Task 143 remains the private triage owner.
No private report details are reproduced here.

## Full-project QA at the original revision

**Failed**, with additional incomplete freshness checks. Every configured check
was executed before source or roadmap edits. The alias stopped at the failed
suite; independent checks collected the remaining outcomes at the same revision.
Later hygiene repairs do not change this baseline judgment.

Toolchain: Elixir 1.20.4, Erlang 29.1 / OTP 29, Dialyzer 6.0.3.

| Command / step | Observed outcome |
| --- | --- |
| `mix precommit.full` (cold) | Exit 1: missing dependencies. |
| `mix deps.get` | Exit 0; mix.lock unchanged. Known cowlib/gun adjudication retained. |
| `mix precommit.full` (bootstrapped) | Exit 1 after nested suite exit 2. |
| `mix format --check-formatted` | Passed within alias. |
| `mix compile --warnings-as-errors` | Passed within alias; cold dependencies emitted dependency warnings. |
| `mix credo --strict --ignore TagTODO,TagFIXME` | Passed: 272 files, 5,028 modules/functions, no issues. |
| `mix doctor --raise` | Passed: 129 modules, 0 failed; docs/moduledocs/specs each 100%. |
| `MIX_ENV=test mix test.json --quiet --cover --cover-threshold 95 --exclude integration --exclude cross_validation --output _build/test/cover.json` | 2,364 passed, 1 failed, 170 excluded, 0 invalid/skipped; 2,535 total; seed 560209. One automatic retry confirmed the failure, zero flaky. |
| Aggregate coverage | 8,435 / 8,635 lines = 97.68%; 95% threshold met. |
| `mix mpp.cover.critical` | Exit 0; every critical module meets its floor. This does not certify suite success. |
| `mix sobelow --skip --exit low` | Exit 0, SCAN COMPLETE. |
| `mix ex_dna --max-clones 0` | Exit 1: one exact recipient-validation clone, 30 nodes, approximately 16 duplicated lines; 123 files; budget 1/0. |
| `mix reach.check --arch --smells --path lib` | Exit 1: architecture OK; one repeated Enum.at/2 lookup finding at USDC Solana line 245. |
| `mix dialyzer.json --quiet` | Exit 2: one pattern_match warning at USDC Solana line 450; successful fallback cannot match remaining error type. |
| `mix agents.check` | Exit 0 but freshness SKIPPED. |
| `mix deps.audit.gated` | Exit 0; scope/populated-mirror guards completed and audit reported no vulnerabilities; freshness SKIPPED. |
| `git diff --check 3bf67c5..HEAD` (before edits) | Passed. |

The one failed assertion was:

```text
assert :ok = SecurityMutations.validate_ledger(ledger, File.cwd!())
left: :ok
right: {:error, :fingerprint_mismatch}
```

Exact skipped-prerequisite output:

```text
[skip] AGENTS.md freshness check: /home/harness/_DATA/code/claude-marketplace/scripts/sync-agents-md.sh not executable (developer-host script, absent in CI).
[skip] advisory-mirror freshness check: /home/harness/_DATA/code/onchain-stack/bin/advisory-freshness.sh not executable (developer-host script, absent in CI).
```

USDC baseline coverage: USDC, Assets, Binding, Profile and Replay 100%;
EVM 98.54%; Solana 97.73%. The XRPL ledger-confirmation log was not another test
failure: suite JSON contains only the mutation-ledger failure. Integration and
cross-validation are excluded by the configured full command. This audit claims
no fresh live-provider validation and did not use any operator server/database,
restart services, deploy, revert or control landing.

Baseline logs were captured under `/tmp/mpp-1df-`: `cold.log`, `deps.log`,
`full.log`, `critical.log`, `sobelow.log`, `clones.log`, `reach.log`,
`dialyzer.log`, `agents.log` and `audit.log`. Relevant output and all outcomes are
preserved above so temporary-log cleanup does not erase the QA judgment.

## Repair validation

Formatted all five changed code/test files and compiled with warnings as errors:
exit 0. Ran:

```sh
MIX_ENV=test mix test.json test/mpp/methods/usdc_test.exs test/mpp/x402/facilitator_test.exs --quiet --no-retry --output /tmp/mpp-1df-focused.json
```

Exit 0: **29 passed**, no failures/exclusions/skips/invalid tests, seed 891200.
Existing success/error coverage exercises both direct profiles, replay handling,
recipient validation, ATA creation and facilitator transport; the two new tests
cover config rejection and invalid indexes. No live contract behavior changed.

Repeated only the failed analyzers affected by the fixes:

- `mix ex_dna --max-clones 0`: exit 0, 123 files, zero clones, budget 0/0.
- `mix reach.check --arch --smells --path lib`: exit 0, architecture OK, no smells.
- `mix dialyzer.json --quiet`: exit 0, warnings empty, zero skipped.

The additional Credo command used `--files-included` with the five edited paths,
but its output reports all 272 files checked: exit 0, 5,026 modules/functions,
no issues. The exact invocation was:

```sh
mix credo --strict --ignore TagTODO,TagFIXME --files-included 'lib/mpp/methods/usdc/{binding,evm,solana}.ex,test/mpp/methods/usdc_test.exs,test/mpp/x402/facilitator_test.exs'
```

`mix format --check-formatted` on the five changed files, `rmap validate
--check-render` and `git diff --check` passed. Full QA was not repeated after
these focused repairs. `.harness/audit.json` records the original integrated
revision and remains uncommitted. Independent reviewer acceptance is not claimed
for this audit's fixes.
