# Post-Merge Audit: ef34147

Audited landed range `422bcdd..ef34147`, covering the Task 27/35/37/45/46/53/57/63/64/67/68/69/70/71 deliveries, release commits through `v0.6.4`, roadmap marker commits, and related dependency/tooling updates.

## Reviewed

- Hygiene scan for debug output, stale TODO/FIXME markers, bare `IO.inspect`/`dbg`, version drift, and whitespace issues.
- Targeted review of docs touched by the range: `README.md`, `CHANGELOG.md`, `docs/security-parity.md`, `docs/tempo-hardening-delta.md`.
- Targeted review of new payment-hardening surfaces: EVM/Tempo replay-store docs, security-parity advisory rows, and project check aliases.
- Cold-build witness: `mix check.dispatch`.

## Findings and Fixes

1. README install snippet still referenced `{:mpp, "~> 0.6.2"}` after `0.6.3` and `0.6.4` landed.
   Fixed by bumping the snippet to `~> 0.6.4`.

2. README EVM section documented hash credentials but did not mention the new EVM transaction-hash replay store from `0.6.3`.
   Fixed by adding a short replay-protection note pointing at `method_config["store"]` and `MPP.Tempo.Store`.

3. `docs/security-parity.md` stated that open-gap details should stay out of the public ledger, but its pending session rows still listed specific open session vectors and a boundary condition.
   Fixed by sanitizing those pending rows while keeping the public advisory/status routing intact.

4. The audit-required check command `mix check.dispatch` did not exist.
   Fixed by adding a `check.dispatch` alias that delegates to the existing `precommit.full` gate. While touching `mix.exs`, also replaced a bare `TODO:` comment with a Task 76-scoped comment.

## Checks

- Initial cold-tree `mix check.dispatch`: failed after compiling dependencies/app because the task alias was missing.
- After the alias fix: `mix check.dispatch` passed. It ran format, warnings-as-errors compile, Credo strict, Doctor, coverage-gated tests (`95.51%`, threshold met), Sobelow, `ex_dna --max-clones 0`, `reach.check --arch --smells`, and `dialyzer.json` with zero warnings.

## Discovery

No follow-up rmap tasks filed; all audit findings were fixed in this commit.

No reviewer rejections were recorded for this range.
