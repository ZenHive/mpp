# Post-merge audit: 24b4181

Reviewed the supplied landed range `ef29ee4^..24b4181`: runtime and test diffs
for WebSocket metering, Stripe subscription lifecycle handling, Tempo recipient
allowlists and subscription authorizations, receipt timing/cache directives,
session vouchers, header escaping, discovery parsing, dependency/toolchain
changes, workflow removal, and the accompanying documentation. This is a
best-effort hygiene review, not a fresh provider-conformance or security audit.
Roadmap status changes were left untouched.

## Findings and fixes

1. README badges and its CI section still advertised the deleted GitHub Actions
   workflows. Removed the badges and documented the actual local gate plus the
   separate integration, cross-validation, and mutation commands.
2. README payment flow still described an immediately attached receipt header.
   Updated it to distinguish the receipt assign from send-time headers on 2xx
   responses, including the cache-control merge.
3. CHANGELOG lacked release notes for the landed WebSocket and Stripe APIs,
   Tempo recipient allowlist and authorization compatibility, receipt behavior,
   voucher acceptance change, and workflow removal. Added an Unreleased section
   without rewriting historical release entries.

No runtime edits were made. The reviewed additions had no leftover `dbg`,
`IO.inspect`, debug prints, or test skips identified by the diff scan. Existing
focused tests cover the changed receipt, voucher, recipient, authorization,
and subscription lifecycle behavior; this audit does not claim those tests
independently establish live-provider correctness.

## Validation

The required `mix check.dispatch` initially stopped because the cold worktree
had no dependencies. After `mix deps.get` succeeded, `mix check.dispatch`
completed with exit 0. Worktree dependencies, builds, and PLTs were not copied
from another tree; dependency tooling used its normal host download caches.

The gate passed formatting, warnings-as-errors project compilation, Credo,
Doctor, tests and coverage (97.12%, threshold 95%), Sobelow, clone and
architecture/smell checks, Dialyzer (zero warnings), and dependency auditing.
The test runner reported 1,933 passed, zero confirmed failures, one flaky test
passing its isolated retry, and 128 excluded integration/cross-validation tests.
Live provider and cross-validation suites were not run by this gate.

The gate explicitly skipped AGENTS.md freshness and advisory-mirror freshness
because their developer-host scripts were unavailable. Exit 0 therefore does
not establish those two freshness checks. Local build evidence is retained in
`.harness/cold-check.log` and `.harness/deps-get.log` (uncommitted).

Documentation-only changes do not require new behavioral tests or a pre-edit
module coverage run. `git diff --check` passed.

## Follow-ups and reviewer feedback

4. The cold check exposed one timing-dependent test (tracked, not fixed here).

Filed Task 113 via `rmap new --from-stdin` for the ConCacheStore invalid-update
test’s dependence on a 25 ms expiry window. The cold run observed one failure
at line 183; the runner’s isolated retry passed. This test was not changed in
the landed range. No additional reruns were performed. The CLI appended the
new task and regenerated roadmap views; existing task statuses were not edited.

No reviewer rejections were supplied,
so there are no false-rejection assessments. The landed merge remains settled.
