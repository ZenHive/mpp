# Post-Merge Audit: 105a362

Reviewed landed range `39b3dd1^..105a362`, covering Task 79's Near Intents
charge method (`39b3dd1` delivery, `0373512` discovery registration,
`adc7773` reviewer fixes, `105a362` roadmap done).

## Reviewed

- `MPP.Methods.NearIntents` plus `OneClick` / `Origin` for dead code, debug
  output, naming, nil-on-the-wire fields, and store/poll documentation.
- Unit and integration tests for hidden failures, over-broad credential
  flunks, and nightly CI wiring.
- `README.md`, `CHANGELOG.md`, `lib/mpp.ex`, `CLAUDE.md`, `AGENTS.md`,
  `SECURITY.md`, and the Hex package description for consumer-facing inventory.
- The required cold-worktree dispatch check.

## Findings and Fixes

1. `CHANGELOG.md` `[Unreleased]` omitted the new hash-only method and the
   settlement problem types it introduced. Added notes for
   `MPP.Methods.NearIntents` and `:settlement_failed` /
   `:settlement_unavailable` / `:settlement_timeout`.
2. Module inventory drifted after discovery registration: `CLAUDE.md` /
   `AGENTS.md` had no Near Intents row, the Hex description still stopped at
   Solana, `SECURITY.md` still listed only Stripe/Tempo/EVM, and
   `MPP.Methods.Shared` did not name Near Intents. Updated those inventories
   and regenerated `AGENTS.md`.
3. `challenge_method_details/1` always serialized `"depositMemo"` even when
   unset, which would put JSON `null` on the 402 wire. Optional memo, poll,
   and origin-RPC keys now use `maybe_put/3`; the unit test refutes the key
   when absent.
4. Nightly `integration.yml` never exported `ETHEREUM_API_URL` or
   `NEAR_INTENTS_ONE_CLICK_JWT`, and `setup_all` flunked the whole Near
   Intents module without a mainnet archive RPC. Mapped the secrets and moved
   the origin-RPC flunk to the test that actually needs it. Quote and refund
   recovery can still run without archive RPC.
5. The README Near Intents section had no mount example and did not say a
   configured store must implement `update/3`. Added a quote-then-Plug snippet
   and documented poll / origin-RPC / store options on the method moduledoc.

The reviewer correctly re-deleted `.cursor/rules/harness-operational.mdc` after
`0373512` restored it (that file is a dispatch-only leak; audit `9f9c9ef`
already removed it once). No leftover debug output or dead library code was
found. No reviewer rejections were recorded for this project or range.

## Follow-Up Tasks

None filed. Nightly origin-RPC coverage still needs the operator to create
GitHub Actions secrets `ETHEREUM_API_URL` (Ethereum mainnet archive) and
optionally `NEAR_INTENTS_ONE_CLICK_JWT`; the workflow now reads them. That is
the same operator-secret step Solana used, not a new product task.

## Cold Check

The worktree began without `deps/` or `_build/`. After `mix deps.get`,
`mix check.dispatch` passed: format, warnings-as-errors compile, Credo
strict, Doctor, 1,581 tests with 105 excluded at 97.33% coverage (Near
Intents 95.56%, threshold met), Sobelow, zero clone findings,
architecture/smell checks, Dialyzer with zero warnings, `AGENTS.md`
freshness, and the dependency security audit.
