# Post-Merge Audit: 4e169a7

Reviewed landed range `7078526..4e169a7` (`4e169a7` tip), covering:

- Task 85 — require EVM `chain_id` (`83c3767` + reviewer `60255e7`)
- Task 84 — Tempo machine-token charge payments (`ab63c4a` + reviewer `4a09272`)
- Task 83 — Aug-2026 wire-parity (`de7decd`)
- Task 33d — MCP client transport (`ab36765`)
- Task 30 — channel state and voucher types (`bc016cf`)
- `25dac1a` — gate typed credential payloads against `credential_types/0`
- `6bb9099` — derive EVM advertised `credentialTypes` from `credential_types/0`
- Surrounding roadmap status / file commits (tasks 21/31/33e/83/84/85/87)

## Reviewed

- Hygiene: dead code, leftover `IO.inspect`/`dbg`, bare TODOs, naming, stale comments.
- Docs inventory: `CHANGELOG.md` Unreleased, README modules + Tempo capabilities + Client section, `CLAUDE.md` / `AGENTS.md` module map, `lib/mpp.ex` Discoverable registry.
- New public surfaces: `MPP.Session.*`, `MPP.Methods.Tempo.MachineToken`, `MPP.Client.MCP` / `Transport.MCP`, EVM `chain_id` requirement, receipt extensions, credential-type gate.
- Cold-build witness: `mix deps.get` then `mix check.dispatch` in this un-warmed worktree.

## Findings and Fixes

1. `CHANGELOG.md` `[Unreleased]` was empty after six consumer-facing deliveries on top of 0.13.0.
   Filled Added / Changed / Fixed notes for MCP client, session types, machine-token, receipt extensions, required EVM `chain_id`, advertised `credentialTypes`, typed-payload gating, case-insensitive auth-params, and parse-time `expires` validation.

2. README / CLAUDE.md / AGENTS.md module maps omitted `MPP.Session.{Channel,Voucher,Store,ETSStore}` and `MPP.Methods.Tempo.MachineToken`. Tempo capabilities also omitted `"machine_token_enabled"`.
   Added the inventory rows and the capabilities clause. Regenerated `AGENTS.md` from `CLAUDE.md`.

3. The README Client section (landed with Task 83's HTTP cross-origin note) did not mention the Task 33d MCP client.
   Added the `MPP.Client.MCP.new/1` + `call/4` snippet.

4. `MPP.Application` started `MPP.Session.ETSStore` but `application.ex` / `mix.exs` comments still described only the ConCache dedup store.
   Updated both comments.

## Accepted leftovers (not fixed)

- `MPP.Client.MCP` is documented on `MPP` but is **not** in the Discoverable registry. descripex derives `short_name` from the last module segment (`Macro.underscore/1`), so `MPP.Client.MCP` and `MPP.Mcp` both resolve to `"mcp"`; registering both would make `MPP.describe(:mcp)` raise as ambiguous. Callers can still use the module directly. Not filed — needs a descripex custom-short-name capability, not an MPP workaround.
- `MPP.Methods.EVM.challenge_method_details/1` still omits `chainId` when `method_details["chain_id"]` is nil. `validate_config!/1` / `Plug.init/1` is the gate (Task 85 tests cover the reject path).
- `Channel.action_to_wire/1` is unused in production code; Task 31 (session credential actions, `in_progress`) is the consumer.

No dead code, leftover debug output, or convention breaks in the landed implementations. No reviewer rejections were recorded for this project/range.

## Follow-Up Tasks

None filed. Task 31 already owns session credential actions; Task 86 owns the EVM signed-transaction credential path; Task 87 owns unpaid OpenAPI routes.

## Cold Check

Worktree had no `deps/` or `_build/` on start. After `mix deps.get`, `mix check.dispatch` passed: format, warnings-as-errors compile, Credo strict, Doctor, coverage-gated tests (`1308` passed, `93` excluded, `97.32%`, threshold met), Sobelow, `ex_dna --max-clones 0`, `reach.check --arch --smells --path lib`, and `dialyzer.json` with `"warnings": []`. Result is recorded in `.harness/audit.json`.
