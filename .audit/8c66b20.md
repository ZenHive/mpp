# Audit of 8c66b20

Range `0f052ee..8c66b20` (11 commits): roadmap filing and status markers for
tasks 119 and 120 (read-only), the task-119 XRPL redemption delivery
(`283221c`, `c6097f8`, `502828d`: bounded Destination/channel leases,
closed-only `redeem/2`, `terQUEUED` handling, validated-hash recovery
logging, lease-timeout config validation), the task-120 Tempo dedup release
delivery (`3a28273`, `b4abe6f`: attempt-token compare-and-delete `delete/2`,
`update/3` fallback, exception/exit barrier for store faults, changelog and
store docs), and the Solana preflight-commitment fix (`8c66b20`).

Codex (gpt-6-astra, static, read-only) reviewed the same diff as the second
opinion; its three findings are items 3-5 below. Claude found items 1-2.

## Findings and fixes

1. `MPP.Methods.XRPL.Session.submit_and_validate/4` returned two success
   shapes through an implicit `with` fall-through: a `terQUEUED` submit comes
   back from the lease as `{:ok, hash, validated}`, which silently skipped the
   `{:ok, hash} <-` clause and the outer `await_validated`. Correct, but only
   by accident of pattern non-match. Made the shapes explicit with
   `validate_submitted/2` clauses and a comment naming the queued-submit path.
   A first pass at this (a `case` inside the `with`) broke the bare `:error`
   passthrough from the sequence/ledger reads; the final shape keeps the
   catch-all clause. Full offline suite green afterwards.
2. An orphaned comment sat between `submit_and_validate/4` and
   `lock_timeout/1` separated by a blank line; attached it to `lock_timeout/1`.
   The `CLAUDE.md` module-map row for `MPP.Methods.XRPL.RedeemLock` still
   described a Destination-only lease; updated to name the per-channel lease
   and bounded acquisition, and regenerated `AGENTS.md`.
3. (Codex, 4/10) The same-channel concurrency test did not force the two
   `redeem/2` calls to overlap, so it could pass without the channel lease.
   Rewrote it on the existing `gate:` RPC stub: A is parked in validation,
   then B must be observed waiting in `RedeemLock.wait_for_owner/3` with no
   Sequence read and the submit counter still at 1 before A is released.
   Negative control: with the channel lease call removed, the test fails
   (B reaches submit); restored.
4. (Codex, 4/10) `docs/xrpl-session.md` said cross-node idempotency "rests on
   a shared session store", which overstates it: two nodes can both read an
   unset hash and both submit before either persists. Reworded to say the
   guarantee holds only after persistence and that nothing coordinates
   concurrent cross-node redemption.
5. (Codex, 2/10) The Unreleased changelog entry said validation waits outside
   the Destination lease without the `terQUEUED` exception that the moduledoc
   and guide document. Added the parenthetical.

Checks on the final tree: `mix check.fast` clean, `mix dialyzer.json` 0
warnings, `mix test.json` offline suite 2052 passed / 0 failed (`:integration`
and `:cross_validation` not run), `sync-agents-md.sh --check` fresh.

No leftover debug output, dead code, or unfixed-security detail in the range.
No follow-up tasks filed.
